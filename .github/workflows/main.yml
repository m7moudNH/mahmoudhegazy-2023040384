on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to docker hub"
        run: docker login --username mahmoudnourhegazy --password dckr_pat_S2PmgxTjwWPrOD9VWofZA6yAK20
